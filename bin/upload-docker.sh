#! /bin/bash
echo "UPLOAD DOCKER"
docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
docker image tag "$DOCKER_IMAGE" "$DOCKER_USERNAME/$DOCKER_IMAGE:$DOCKER_TAG"
docker push "$DOCKER_USERNAME/$DOCKER_IMAGE:$DOCKER_TAG"